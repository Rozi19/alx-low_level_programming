#include "main.h"
/**
 * add -  adds two integers and returns the result.
 * @c: pass the first value
 * @d: pass the second value
 * Return: the value
 */
int add(int c, int d)
{
	int r;

	r = c + d;
	return (r);
}
